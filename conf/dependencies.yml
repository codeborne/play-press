self: play -> press 1.2.2

require:
    - play 1.2+
    - com.asual.lesscss -> lesscss-engine 1.3.3:
        transitive: false
    - local -> yuicompressor 2.4.8

repositories:
    - local:
        type:       local
        artifact:   ${application.path}/jars/[module]-[revision].jar
        contains:
            - local
